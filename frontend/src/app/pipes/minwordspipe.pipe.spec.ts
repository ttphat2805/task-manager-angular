import { MinwordspipePipe } from './minwordspipe.pipe';

describe('MinwordspipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinwordspipePipe();
    expect(pipe).toBeTruthy();
  });
});
