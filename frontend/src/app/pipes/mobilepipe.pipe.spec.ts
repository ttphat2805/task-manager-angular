import { MobilepipePipe } from './mobilepipe.pipe';

describe('MobilepipePipe', () => {
  it('create an instance', () => {
    const pipe = new MobilepipePipe();
    expect(pipe).toBeTruthy();
  });
});
